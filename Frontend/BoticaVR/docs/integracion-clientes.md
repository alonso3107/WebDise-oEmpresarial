# Clientes

- CRUD: `/api/v1/clientes/`
- Historial: `/api/v1/ventas/?cliente_id={id}`

El formulario conserva `nombres` y `apellidos` por separado para respetar el
contrato backend. El DNI no se modifica después del registro.
