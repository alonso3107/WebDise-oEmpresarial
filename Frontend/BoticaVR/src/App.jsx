// ============================================================
// BoticaVR — App (no usado directamente — rutas van por RouterProvider)
// Se mantiene por si se necesita en pruebas unitarias.
// ============================================================

import { Outlet } from 'react-router-dom';

function App() {
  return <Outlet />;
}

export default App;
